export type CardContentType = {
  icon: string;
  title: string;
  description: string;
  cardType?: "primary";
};

export type sliderContentType = {
  comment: string;
  name: string;
  company?: string;
};
